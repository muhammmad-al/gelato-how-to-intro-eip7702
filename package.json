{
  "name": "eip-7702-demo",
  "version": "1.0.0",
  "description": "EIP-7702 Account Abstraction Demo with Hardhat",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts --network sepolia",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "demo": "ts-node demo.ts",
    "test": "hardhat test"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/dotenv": "^6.1.1",
    "@types/node": "^20.0.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
